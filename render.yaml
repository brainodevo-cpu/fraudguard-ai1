services:
  - type: web
    name: fraudguard-ai
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --workers 1 --timeout 600 --graceful-timeout 600
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0